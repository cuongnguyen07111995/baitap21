extends layouts

block content
  form(method='post',action='/signup',encty='applcation/x-www-form-urlencoded')
  h3 Sign up
  if exists
      .error
        |That user exists. Are you trying to
        a(href="/login") Login
  if invalid
       .errors Please ente a valid email  address and password
  div
    ul
      li
          label(for='email') Email
          input#email(name='email')
      li
          label(for='pass') Password
          input#pass(name='pass')
      li
          label(for='ten') Ten
          input#ten(name='ten')
  input(type='submit',value='Sign Up')